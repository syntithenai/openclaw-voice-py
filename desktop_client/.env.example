# OpenClaw desktop client defaults
# Copy to .env and adjust if needed.

DESKTOP_GATEWAY_URL=https://localhost

DESKTOP_DEFAULT_TTS_MUTED=false
DESKTOP_DEFAULT_CONTINUOUS_MODE=false
DESKTOP_RECONNECT_DELAY_S=1.5
