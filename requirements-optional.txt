# Optional dependencies for emotion detection, wake word detection, PyTorch, and improved VAD

# Wake word detection with Precise (Mycroft)
# Required for ARMv7 (Raspberry Pi 32-bit) wake word detection
precise-runner

# Wake word detection with OpenWakeWord
# Requires onnxruntime which is not available for Python 3.11 on ARMv7 (Raspberry Pi 32-bit)
# Only tested on x86_64 systems
onnxruntime
openwakeword

# Emotion detection (SenseVoice)
# Only required if EMOTION_ENABLED=true
funasr

# PyTorch and Silero VAD for TTS cut-in detection
# NOTE: 5.2.0 is no longer published on PyPI; 5.1.2 is the latest compatible 5.x release.
# Keep torchaudio pinned to the torch major/minor to avoid long resolver backtracking during Docker builds.
torch==2.1.0
torchaudio==2.1.0
silero-vad==5.1.2
